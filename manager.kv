<Manager>:

    orientation: 'vertical'
    image_box: image_box_

    size_hint: (1, 1)

    canvas.before :
        Rectangle :
            source:'images/background.png'
            size : self.size
            pos : self.pos

    Image:
        id: image_box_
        source:''
        allow_stretch: True
        size_hint: (1, 1)
